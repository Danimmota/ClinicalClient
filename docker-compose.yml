services:
  db-client:
    image: mysql:8.0
    container_name: db-client
    environment:
      MYSQL_ROOT_PASSWORD: 3J^,+VGAg64]GpDaWx8V
      MYSQL_DATABASE: clinical_db
    ports:
      - "3307:3306"
    volumes:
      - db_data:/var/lib/mysql

#  rabbitmq:
#    image: rabbitmq:3-management
#    container_name: rabbitmq
#    ports:
#      - "5672:5672"      # Porta de comunicação AMQP
#      - "15672:15672"    # Painel web
#    networks:
#      - clinic-network

  client-service:
    build:
      context: .
    container_name: client-service
    depends_on:
      - db-client
#      - rabbitmq
    ports:
      - "8081:8081"
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://db-client:3307/clinical_db
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: 3J^,+VGAg64]GpDaWx8V

volumes:
  db_data:

